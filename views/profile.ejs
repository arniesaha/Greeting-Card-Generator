<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>

    <style>
      body {
        background: dodgerBlue;
        font-family: courier;
        color: white;
        padding: 30px; }
      h1 {
        font-size: 48px;
        text-transform: uppercase;
        letter-spacing: 2px;
        text-align: center }
        h1 {
          font-size: 30px;
          text-transform: uppercase;
          letter-spacing: 2px;
          text-align: center }
        p, li {
          font-size: 16px;
          text-align: center;
        }
      </style>
  </head>
  <body>
    <% include partials/nav.ejs %>

    <h1>Webpage of <%= person %>'s Profile</h1>
    <p>Age: <%= data.age %></p>
    <p>Job: <%= data.job %></p>
    <h2>Hobbies</h2>
    <ul>
      <% data.hobbies.forEach(function(item){ %>
        <li> <%= item %> </li>
      <% }); %>
    </ul>

  </body>
</html>
